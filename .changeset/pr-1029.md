<!-- auto-generated by auto-changeset workflow -->
---
"@bike4mind/common": patch
"@bike4mind/services": patch
"@bike4mind/utils": patch
---

keep uploaded documents in notebook context so the model can use them

Also changes the ChatCompletionFeature.getContextMessages signature (drops the unused max_tokens, adds attachedFileTokenBudget) and the processFabFilesServer budget semantics (sized from the model input window and divided across text files, instead of the output-token cap).
