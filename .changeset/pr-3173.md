<!-- auto-generated by auto-changeset workflow -->
---
"@bike4mind/cli": patch
---

confine sandbox writable root and honor network.enabled

Behavior change: with the sandbox enabled, network egress is now fail-closed - IP egress is denied by default (local unix sockets like ssh-agent still work). Turn filtered egress back on with the new `/sandbox:network on` command, or set `sandbox.network.enabled` in config.
