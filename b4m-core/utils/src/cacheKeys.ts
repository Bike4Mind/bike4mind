/**
 * This is where we define the keys for the cache
 */
export const CacheKeys = {
  // salesforceQuery: ({ object, pageSize, pageNumber }: { object: string; pageSize: number; pageNumber: number }) =>
  //   `salesforce:query:${object}:${pageSize}:${pageNumber}`,
};
