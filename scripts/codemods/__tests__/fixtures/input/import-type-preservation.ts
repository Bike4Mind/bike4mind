/* eslint-disable */
import type { ILogger } from '@bike4mind/utils';
import { Logger, type LogLevel } from '@bike4mind/utils';

const log: ILogger = new Logger('test');
const level: LogLevel = 'info';
