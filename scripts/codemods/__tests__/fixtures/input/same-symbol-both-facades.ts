/* eslint-disable */
import { Logger } from '@bike4mind/utils';
import { AuthTokenGeneratorService } from '@bike4mind/services';

const log = new Logger('auth');
const svc = new AuthTokenGeneratorService();
